name: Polaris Debug

on:
  push:
    branches: ["main"]
    paths:
      - "**.cpp"
      - "**.h"
      - "**.hpp"
      - "**CMakeLists.txt"
      - "**.cmake"
  pull_request:
    branches: ["main"]
    paths:
      - "**.cpp"
      - "**.h"
      - "**.hpp"
      - "**CMakeLists.txt"
      - "**.cmake"
  workflow_dispatch:

env:
  BUILD_CONFIGURATION: Debug
  BUILD_DIR: cmake-build-debug

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up MSVC Developer Environment
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install Ninja
        uses: seanmiddleditch/gha-setup-ninja@v3

      - name: Configure CMake (clang-cl + Ninja)
        run: |
          $env:CC = "clang-cl"
          $env:CXX = "clang-cl"
          cmake -B ${{ env.BUILD_DIR }} -G Ninja -DCMAKE_BUILD_TYPE=${{ env.BUILD_CONFIGURATION }} -DCMAKE_C_COMPILER=clang-cl -DCMAKE_CXX_COMPILER=clang-cl

      - name: Build with Ninja
        run: |
          cmake --build ${{ env.BUILD_DIR }} --config ${{ env.BUILD_CONFIGURATION }}

      - name: Rename DLL + PDB
        shell: pwsh
        run: |
          $dll = "${{ github.workspace }}\${{ env.BUILD_DIR }}\Polaris.dll"
          $pdb = "${{ github.workspace }}\${{ env.BUILD_DIR }}\Polaris.pdb"

          if (Test-Path $dll) {
            Rename-Item -Path $dll -NewName "PolarisDebug.dll" -Force
          } else {
            Write-Error "Polaris.dll not found"
            exit 1
          }

          if (Test-Path $pdb) {
            Rename-Item -Path $pdb -NewName "PolarisDebug.pdb" -Force
          }

      - name: Compute build timestamp
        shell: bash
        run: |
          TS=$(date -u +"%Y%m%d-%H%M%S")
          echo "TIMESTAMP=$TS" >> "$GITHUB_ENV"

      - name: Create timestamped archival release
        uses: softprops/action-gh-release@v2.3.2
        with:
          tag_name: debug-${{ env.TIMESTAMP }}
          name: Polaris Debug ${{ env.TIMESTAMP }}
          body: |
            ### Automated timestamped debug build

            Built at: ${{ env.TIMESTAMP }} UTC

            Includes:
            - PolarisDebug.dll
            - PolarisDebug.pdb
          prerelease: true
          generate_release_notes: false
          files: |
            ${{ github.workspace }}/cmake-build-debug/PolarisDebug.dll
            ${{ github.workspace }}/cmake-build-debug/PolarisDebug.pdb

      - name: Recreate debug release to refresh date
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete debug -y || true
          git push origin :refs/tags/debug || true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2.3.2
        with:
          tag_name: debug
          name: Polaris Debug
          body: |
            ### Latest Automated Debug Build

            ⚠ Debug builds may contain bugs or be slow.

            Includes:
            - PolarisDebug.dll
            - PolarisDebug.pdb
          prerelease: true
          make_latest: true
          files: |
            ${{ github.workspace }}/cmake-build-debug/PolarisDebug.dll
            ${{ github.workspace }}/cmake-build-debug/PolarisDebug.pdb
